// import { selector } from 'recoil'
// import { likeState } from '../atoms/likeState'

export const likeCountSelector = (postId: string) => {}
// selector<number>({
//     key: `likeCount_${postId}`,
//     get: async function likeCount({ get }) {
//         const isLiked = get(likeState)
//         return isLiked ? 1 : 0
//     },
// })
